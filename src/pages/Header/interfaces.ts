export interface HeaderScrollProps {
    scroll: boolean;
}

export interface HeaderPropsActive {
    isActive: boolean;
}

export interface HeaderLinkColor {
    hover: string;
}

export interface MenuProps {
    color?: string;
    arr: Array<any>;
}
